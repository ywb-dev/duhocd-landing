type Duhocsinh @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  dob: AWSDate
  sex: String
  phone: String
  email: String! @primaryKey
  residenceAddress: String
  hometownAddress: String
  degree: String
  averageScoreC3: String
  scoreGPA: String
  universityWant: String
  timeStudyAbort: AWSDate
  question: String
} 